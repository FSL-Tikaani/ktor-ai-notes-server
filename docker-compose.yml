services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - db_data:/app/db
    env_file:
      - .env

volumes:
  db_data:
